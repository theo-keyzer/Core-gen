----------------------------------------------------------------
Actor main
----------------------------------------------------------------

Its .names get_args

Add set.split., Iaa ${3}
Add set.split., Ibb ${4}

-Add set Ibb

C ${._set}
C ${._set.Ibb}
C ${..Abc}
C ${._set.Args}
C

Add set zz,yy,bb abc
C ${._set}

This set test
This set test2
This set test3

C

Add set P a
Clear set P
This set.Args perm

----------------------------------------------------------------
Actor perm
----------------------------------------------------------------

This set.Args perm2

----------------------------------------------------------------
Actor perm2 Set .perm.._str != ${._str}
----------------------------------------------------------------

This set.Args perm3 ${.perm.._str}n,${._str}
Add set P ${.perm.._str}n,${._str}

C 1 ${.perm.._str}  ${._str}

----------------------------------------------------------------
Actor perm3 Set
----------------------------------------------------------------

-C 2 ${._arg}  ${._set.P}

This set.P perm4 ${._arg}

----------------------------------------------------------------
Actor perm4 Set ._str is ${._arg}
----------------------------------------------------------------

-C 3 ${._str}   is ${._arg}

Break cmds of perm2


----------------------------------------------------------------
Actor get_args . ._key !in 0,1,2
----------------------------------------------------------------

C ${._str}  - ${._key}

Add set.split., Arg${._key} ${._str}
Add set Args Arg${._key}


----------------------------------------------------------------
-Actor test . ._str ?
Actor test . ._key in Iaa,Ibb
----------------------------------------------------------------

C  test ${.:join}  ${._key}

----------------------------------------------------------------
Actor test2 . ._key has yy
----------------------------------------------------------------

C  test2 ${.:join}  ${._key}

----------------------------------------------------------------
Actor test3 . ._key in ${._set.Args}
----------------------------------------------------------------

C  test3 ${.:join}  ${._key}

