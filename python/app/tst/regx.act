--------------
Actor main
--------------

Add var str A(i,j) = B(i,j,k) * c(k);

C ${._var.str}
C

That regx string ${._var.str} response_list (\w+)\(([\w,\s]+)\)\s+=\s+(\w+)\(([\w,\s]+)\)\s*\*\s*(\w+)\(([\w,\s]+)\)

Du tst_act A
Du tst_act B

----------------------------------
Actor response_list .
----------------------------------

C here1 ${._type}  ${._key}  ${.}
C here1 ${.:3}  - ${.:3:split:1}
C ${.:0}(${.:1}) = ${.:2}(${.:3}) * ${.:4}(${.:5})

Its . response T1,e1,T2,e2,T3,e3

In on

Cs {

Its . collect T1,e1,T2,e2,T3,e3

C
C }

In off

C ${._ins}

That json string ${._ins} str_act

----------------------------------
Actor response .
----------------------------------

C here2 ${._type} ${._arg:split:-} is ${.}

----------------------------------
Actor collect .
----------------------------------

C ${.1., }
Cs  "${._arg:split:-}":"${.}"


----------------------------------------------------------------
Actor str_act
----------------------------------------------------------------

C ${T1}(${e1}) = ${T2}(${e2}) * ${T3}(${e3})

----------------------------------------------------------------
Actor tst_act . ._arg regex A
----------------------------------------------------------------

C is A

----------------------------------------------------------------
Actor tst_act . ._arg regex B
----------------------------------------------------------------

C is B

----------------------------------------------------------------
Actor tst_act . ._arg regex .
----------------------------------------------------------------

C is any

