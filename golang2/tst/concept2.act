----------------------------------------------------------------
Actor main
----------------------------------------------------------------

Add.list _:Unique

C <!DOCTYPE html>
C <html>
C <body>

All Topic topic ${2}

C </body>
C </html>

----------------------------------------------------------------
Actor topic Topic .main.2 = *
----------------------------------------------------------------

C
C <${tag}>${desc}</${tag}>
C <a name=tag_${kMe}></a>

Its T t
Break


----------------------------------------------------------------
Actor topic Topic name in ${.main.2}
----------------------------------------------------------------

Add.break _.Unique ${kMe}
Its level.left topic_left

C
C <${tag}>${desc}</${tag}>
C <a name=tag_${kMe}></a>

Its T t
Its level.right topic_right
Break

----------------------------------------------------------------
Actor topic_left
----------------------------------------------------------------

Add.break _.Unique ${kMe}

C
C <${tag}>${desc}</${tag}>
C <a name=tag_${kMe}></a>

Its T t
Break

----------------------------------------------------------------
Actor topic_right Topic name !in ${.main.2}
----------------------------------------------------------------

Add.break _.Unique ${kMe}

C
C <${tag}>${desc}</${tag}>
C <a name=tag_${kMe}></a>

Its T t
Break

----------------------------------------------------------------
Actor t T desc != .
----------------------------------------------------------------

C ${desc}


----------------------------------------------------------------
Actor t T .main.3 ?= include
Actor t T file &!= .
----------------------------------------------------------------

Add.file _:inc ${file}
C ${_.inc}

----------------------------------------------------------------
Actor topic_is Topic name in ${.main.2}
----------------------------------------------------------------
-C - xx
Add _:Is true
Break loop

----------------------------------------------------------------
Actor links Topic name != .
Actor links Topic name &!in ${.main.2}
----------------------------------------------------------------

Add.break _:Unique ${name}

C <a href="${name}.html#tag_${.topic.kMe}">${name}</a>

