----------------------------------------------------------------
Actor main
----------------------------------------------------------------

Add.map    _:M
Add.map    _:G

All Matrix mat_collect


This _.M. mat_all_list

-C ${_}
C

This _.G. graph_all check

C



---------------------------------------------------------------
Actor mat_collect Matrix search in 1,2,3,4,5
----------------------------------------------------------------

-C ${search}  ${a}-${b}

Add.list _.M:${search}

Add _.M.${search} ${a}-${b}
Add _.M.${search} ${b}-${c}

----------------------------------------------------------------
Actor mat_all_list List
----------------------------------------------------------------

This . mat_all

----------------------------------------------------------------
Actor mat_all String
----------------------------------------------------------------

-C  a ${.}

All Graph graph_collect ${.}

---------------------------------------------------------------
Actor graph_collect Graph name = ${._arg}
----------------------------------------------------------------

-C    s ${search}  ${name}

Add.list _.G:${search}

Add.check _.G.${search} ${name}

----------------------------------------------------------------
Actor graph_all List . = [A-B, B-C]
----------------------------------------------------------------

C  all ${._key} ${.} 

Break

----------------------------------------------------------------
Actor graph_all List . = [B-C, B-A, C-A]
----------------------------------------------------------------

C  all ${._key} ${.} 

Break

-----------------------------------------------------------------
Actor graph_all List . = [C-B, A-C, C-A]
----------------------------------------------------------------

C  all ${._key} ${.} 

Break

----------------------------------------------------------------
Actor graph_all List
----------------------------------------------------------------

C  no ${._key} ${.} 


