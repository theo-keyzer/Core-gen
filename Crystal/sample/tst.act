---------------------------------
Actor main .
---------------------------------

All Type.User tbls

---------------------------------
Actor tbls .
---------------------------------

Its Attr attr

---------------------------------
Actor attr Attr relation in dropdown,lookup,view
---------------------------------

C ${name}  ${Where_attr.from_id}  ${Where_attr.from_id.desc}  ${Where_attr.from_id.parent.name}

Its Where_attr.from_id attr_from

---------------------------------
Actor attr_from Attr
---------------------------------

C  From : ${name}    ${desc}  ${parent.name}

Its Where_from_id.attr attr_from_where

---------------------------------
Actor attr_from_where Attr
---------------------------------

C   From where : ${name}    ${desc}  ${parent.name}

