----------------------------------------------------------------
Actor main
----------------------------------------------------------------

All Matrix mat_collect ${2}
All Group.M mat_all

All Group.G graph_all ${2}

Group pr M
C
Group pr G

---------------------------------------------------------------
-Actor mat_collect Matrix search in ${.arg}
Actor mat_collect Matrix
----------------------------------------------------------------

Group add M ${search} ${a} -${b}
Group add M ${search} ${b} -${c}

----------------------------------------------------------------
Actor mat_all
----------------------------------------------------------------

C  ${value}

Its value mat_items

----------------------------------------------------------------
Actor mat_items
----------------------------------------------------------------

C  ${value}

All Graph graph_collect ${value}

---------------------------------------------------------------
-Actor graph_collect Graph name = ${.arg}
Actor graph_collect Graph
----------------------------------------------------------------

-C ${search}

Group add G ${search} ${name}

----------------------------------------------------------------
Actor graph_all . value = A-B,B-C
----------------------------------------------------------------

C  all ${key}  ${value} 

Break

----------------------------------------------------------------
Actor graph_all . value = B-A,B-C,C-A
----------------------------------------------------------------

C  all ${key}  ${value} 

Break

-----------------------------------------------------------------
Actor graph_all . value = A-C,C-A,C-B
----------------------------------------------------------------

C  all ${key}  ${value} 

Break

-----------------------------------------------------------------
-Actor graph_all Group ${.arg} = check
Actor graph_all .
----------------------------------------------------------------

C  no  ${key}  ${value}  ${.arg} 

