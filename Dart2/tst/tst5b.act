----------------------------------------------------------------
Actor main
----------------------------------------------------------------

All Matrix mat_collect 
This _. graph_all check

---------------------------------------------------------------
Actor mat_collect Matrix search in 1,2,3,4,5,6,7
----------------------------------------------------------------

-C ${search}  ${a}-${b}

All Graph graph_collect ${a}-${b}
All Graph graph_collect ${b}-${c}

---------------------------------------------------------------
Actor graph_collect Graph name = ${._arg}
----------------------------------------------------------------

-C    s ${search}  ${name}

Add.list _:${search}

Add.check _.${search} ${name}

----------------------------------------------------------------
Actor graph_all List . = [A-B, B-C]
----------------------------------------------------------------

C  all ${._key} ${.} 

Break

----------------------------------------------------------------
Actor graph_all List . = [B-C, B-A, C-A]
----------------------------------------------------------------

C  all ${._key} ${.} 

Break

-----------------------------------------------------------------
Actor graph_all List . = [A-C, C-B, C-A]
----------------------------------------------------------------

C  all ${._key} ${.} 

Break

----------------------------------------------------------------
Actor graph_all Lisst
----------------------------------------------------------------

C  no ${._key} ${.} 


