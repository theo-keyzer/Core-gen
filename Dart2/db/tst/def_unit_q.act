----------------------------------------------------------------
Actor main
----------------------------------------------------------------

All Comp comp

----------------------------------------------------------------
Actor comp Comp
----------------------------------------------------------------

C
C Comp ${name} ${nop:eol} ${parent:eol} ${find:eol} ${doc}

Its Element element
Its Ref ref
Its Ref2 ref2
Its Refu refu
Its Refq refq
Its Join join_q

----------------------------------------------------------------
Actor element Element
----------------------------------------------------------------

C  Element ${name} ${mw} ${mw2:eol} ${pad:eol} ${doc}

----------------------------------------------------------------
Actor ref Ref
----------------------------------------------------------------

C  Ref ${element} ${comp} ${opt:eol}

----------------------------------------------------------------
Actor ref2 Ref2
----------------------------------------------------------------

C  Ref2 ${element} ${comp} ${element2} ${opt:eol}

----------------------------------------------------------------
Actor refu Refu
----------------------------------------------------------------

C  Refu ${element} ${comp} ${element2} ${comp_ref} ${element3} ${opt:eol}

----------------------------------------------------------------
Actor refq Refq
----------------------------------------------------------------

C  Refq ${element} ${comp} ${element2} ${comp_ref} ${opt:eol}

----------------------------------------------------------------
Actor join_q Join
----------------------------------------------------------------

Its parent.parent.Element look

----------------------------------------------------------------
Actor look Element mw in R1,RS
----------------------------------------------------------------

All Comp look2 ${Ref_element.comp}
Break loop

----------------------------------------------------------------
Actor look Element mw in Q1,QS
----------------------------------------------------------------

All Comp look2 ${Refq_element.comp}
Break loop

----------------------------------------------------------------
Actor look2 Comp parent = ${._arg}
Actor look2 Comp name &= ${.join_q.comp}
----------------------------------------------------------------

C
C  Element ${.join_q.element:l}    Q1 ${.join_q.comp} 
C Refq ${.join_q.element} ${.join_q.comp} ${.look.name} ${.look.parent.name} ?

Break


