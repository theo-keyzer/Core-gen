----------------------------------------------------------------
Actor main
----------------------------------------------------------------

C ----------------------------------------------------------------
C Actor main
C ----------------------------------------------------------------
C
C Dbconn localhost postgres postgres $${2}
C

All Comp call

C 

All Comp comps

----------------------------------------------------------------
Actor call Comp parent = .
----------------------------------------------------------------

C All ${name} ${name}

----------------------------------------------------------------
Actor comps Comp parent = .
----------------------------------------------------------------

C ----------------------------------------------------------------
C Actor ${name}
C ----------------------------------------------------------------
C
Cs Add.execute _:${name} Insert into ${name} (parent_id

Its Element ele

Cs ) values (-1

Its Element elev

C ) returning pk_id

All Comp childs_call ${name}

C

All Comp childs ${name}

----------------------------------------------------------------
Actor ele
----------------------------------------------------------------

Cs , "${name}"

----------------------------------------------------------------
Actor elev
----------------------------------------------------------------

Cs , '$${${name}}'

----------------------------------------------------------------
Actor childs_call Comp parent = ${._arg}
----------------------------------------------------------------

C Its ${name} ${name}

----------------------------------------------------------------
Actor childs Comp parent = ${._arg}
----------------------------------------------------------------

C ----------------------------------------------------------------
C Actor ${name}
C ----------------------------------------------------------------
C
Cs Add.execute _:${name} Insert into ${name} (parent_id

Its Element ele


Cs ) values ($${_.${parent.name}:0:0}

Its Element elev

C ) returning pk_id
C
All Comp childs_call ${name}

C


All Comp childs ${name}


