----------------------------------------------------------------
Actor main
----------------------------------------------------------------

All Comp comp

----------------------------------------------------------------
Actor comp Comp
----------------------------------------------------------------

C
C Comp ${name} ${nop:eol} ${parent:eol} ${find:eol} ${doc:eol}

Its Element element
Its Ref ref
Its Ref2 ref2
Its Refu refu
Its Refq refq
Its Join join

----------------------------------------------------------------
Actor element Element
----------------------------------------------------------------

C  Element ${name} ${mw} ${mw2:eol:c} ${pad:eol} ${doc}

----------------------------------------------------------------
Actor ref Ref
----------------------------------------------------------------

C  Ref ${element} ${comp} ${opt:eol}

----------------------------------------------------------------
Actor ref2 Ref2
----------------------------------------------------------------

C  Ref2 ${element} ${comp} ${element2} ${opt:eol}

----------------------------------------------------------------
Actor refu Refu
----------------------------------------------------------------

C  Refu ${element} ${comp} ${element2} ${comp_ref} ${element3} ${opt:eol}

----------------------------------------------------------------
Actor refq Refq
----------------------------------------------------------------

C  Refq ${element} ${comp} ${element2} ${comp_ref} ${opt:eol}

----------------------------------------------------------------
Actor ref3 Ref3
----------------------------------------------------------------

C  Ref3 ${element} ${comp} ${element2} ${comp_ref} ${element3} ${opt:eol}

----------------------------------------------------------------
Actor join Join
----------------------------------------------------------------

Its parent.Element m_look

----------------------------------------------------------------
Actor m_look Element mw in R1,RS,F1
----------------------------------------------------------------

All Comp m_look3 ${Ref_element.comp}

----------------------------------------------------------------
Actor m_look Element mw in Q1,QS
----------------------------------------------------------------

All Comp m_look3 ${Refq_element.comp}

----------------------------------------------------------------
Actor m_look3 Comp name = ${._arg}
----------------------------------------------------------------

Its Element m_look4

----------------------------------------------------------------
Actor m_look4 Element mw in R1,RS,F1
----------------------------------------------------------------

All Comp m_look5 ${Ref_element.comp}

----------------------------------------------------------------
Actor m_look5 Comp parent = ${._arg}
Actor m_look5 Comp name &= ${.join.comp}
----------------------------------------------------------------

C
C  Element ${.join.element:l}    M1 ${.join.comp} 
C Ref3 ${.join.element} ${.join.comp} ${.m_look.name} ${.m_look3.name} ${.m_look4.name} check

Break loop of join


